# xrd-peak-analyzer
Takes XRD data, automatically finds peaks, calculates d-spacings using Bragg's Law, fits Gaussian profiles to get FWHM, and outputs a publication-quality dark-theme plot plus a CSV of results.
